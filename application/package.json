{
    "dependencies": {
        "@types/aws-lambda": "^8.10.61",
        "aws-lambda": "^1.0.6"
    },
    "devDependencies": {
        "@types/node": "^14.14.37",
        "aws-sdk": "^2.740.0",
        "ts-jest": "^26.5.4",
        "ts-node": "^9.0.0",
        "typescript": "^4.1.5"
    },
    "scripts": {
        "build": "tsc; npm run clean && tsc --project ./tsconfig.json",
        "clean": "rm -rf dist/*"
    }
}